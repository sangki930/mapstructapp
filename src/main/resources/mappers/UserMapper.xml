<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.UserDao">

    <insert id="insertUser" parameterType="com.example.demo.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password)
        VALUES (#{username}, #{email}, #{password})
    </insert>

    <select id="findById" resultType="com.example.demo.domain.User">
        SELECT id, username, email, password
        FROM users
        WHERE id = #{id}
    </select>
    
    <select id="findAll" resultType="com.example.demo.domain.User">
        SELECT id, username, email, password
        FROM users
    </select>

</mapper>
